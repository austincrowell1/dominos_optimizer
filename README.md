# dominos_optimizer
A utility that find the optimal first move in Mexican Train Dominos via algorithm.

todo:
    add error handling for user input
    add logic to handle holding doubles?
    build a picture based GUI eventually to input dominos